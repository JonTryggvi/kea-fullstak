var jUser = {}
var mUser = require( __dirname + '/../models/user.js')
jUser.getUserName = (fCallback) => {
//  TODO: Connect to the datavase
  //  TODO: Query the data to get a user
  /*
    
    stmt = 'SELECT * FROM users LIMIT 1'
    db.query(stmt, (err, aUsers) => {
      if(err) {
        return callBack(true, [])
      }
      
    })
  
  */

 

  
   
  //   var sUserName = jRandomUser.name
  //   return fCallback(false, sUserName)
  // })

}


module.exports = jUser