process.on('uncaughtException', (ex) => {
  console.log('Trouble, something is really wrong!-'+ex);
  
})




var jUer = {};
// first key is undefined , after that server dies

try {
  var jMyUser = jUser.name.fullname

  setInterval(function () { console.log('x'); }, 1000)
} catch (ex) {
  console.log('server1.js - ex -1')
}
